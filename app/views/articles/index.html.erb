<p id="notice"><%= notice %></p>

<h1 style='text-align: center;'>Articles (<%= @articles.count %>)</h1>
<% if @articles.length > 0 %>
  <% @articles.each do |article| %>
    <p><%= link_to article.title, article %></p>
  <% end %>
<% else %>
  <h2> No articles found... </h2>
<% end %>
